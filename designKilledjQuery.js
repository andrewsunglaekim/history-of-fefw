





              let complexity = 0;
              const canJQuery = 100;

              while (complexity < canJQuery) {
                createJqueryCode();
                designSickNewFeature();
              }

              frontEndInnovates();

              function createJqueryCode() {
                console.log('awesome new jQuery feature')
              }

              function designSickNewFeature() {
                console.log('what if we do this!')
                complexity++;
              }

              function frontEndInnovates() {
                console.log('And so the front end framework was born')
              }
